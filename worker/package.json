{
	"name": "contextscripture-book-gate",
	"version": "1.0.0",
	"private": true,
	"description": "Cloudflare Worker that gates the Context Matters PDF behind an email submission.",
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20240725.0",
		"typescript": "^5.4.5",
		"wrangler": "^3.78.0"
	}
}
